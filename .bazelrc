import %workspace%/jax_rocm_plugin/.bazelrc

build:rocm --test_tag_filters=cpu,gpu,-tpu,-config-cuda-only
build:rocm --build_tag_filters=cpu,gpu,-tpu,-config-cuda-only
build:rocm --action_env=TF_ROCM_AMDGPU_TARGETS=gfx908,gfx90a,gfx942
build:rocm --@jax//jax:build_jaxlib=true

test:rocm --test_tag_filters=cpu,gpu,-tpu,-config-cuda-only
