name: Build wheels

on:
  workflow_call:
    inputs:
      python-versions:
        required: true
        type: string
      rocm-version:
        required: true
        type: string
      rocm-build-job:
        required: false
        type: string
      rocm-build-num:
        required: false
        type: string
      runner-label:
        required: false
        type: string
      artifact-prefix:
        required: false
        default: 'plugin_wheels'
        type: string
      jax-repo:
        description: >-
          JAX repository to checkout for jaxlib build. Leave empty to skip
          (JAX 0.9.1+ uses upstream PyPI jaxlib).
        required: false
        type: string
        default: ''
      jax-ref:
        description: >-
          JAX git ref (branch, tag, or commit) to checkout for jaxlib build.
          Leave empty to skip (JAX 0.9.1+ uses upstream PyPI jaxlib).
        required: false
        type: string
        default: ''
      builder-image:
        required: false
        type: string
        default: ''
    secrets:
      rbe_ci_key:
        required: true
      rbe_ci_cert:
        required: true

jobs:
  build-plugin-wheels:
    runs-on: ${{ fromJSON(inputs.runner-label) }}
    steps:
      - name: Clean up old runs
        run: |
          ls -lah
          # Make sure that we own all of the files so that we have permissions to delete them
          docker run --rm -v "./:/rocm-jax" ubuntu \
            /bin/bash -c "shopt -s dotglob; chown -R $UID /rocm-jax/* || true"
          # Remove any old work directories from this machine
          rm -rf * || true
          ls -lah
          # Clean up any docker stuff that's more than a week old
          docker system prune -a --filter "until=168h"
          # Stop any containers running for more than 12 hours. No CI job should take this long.
          docker ps --format="{{.RunningFor}} {{.Names}}" | grep hours \
              | awk -F: '{if($1>12)print$1}' | awk ' {print $4} ' | xargs docker stop || true
      - name: Print system info
        run: |
          whoami
          printenv
          df -h
          rocm-smi || true
          rocm-smi -a || true
          rocminfo | grep gfx || true
      - uses: actions/checkout@v4
      - name: Checkout JAX repo for jaxlib build
        if: ${{ inputs.jax-ref != '' }}
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.jax-repo }}
          ref: ${{ inputs.jax-ref }}
          path: jax
      - name: Authenticate to GitHub Container Registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" \
            | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Get RBE cluster keys
        env:
          RBE_CI_CERT: ${{ secrets.rbe_ci_cert }}
          RBE_CI_KEY: ${{ secrets.rbe_ci_key }}
        run: |
          echo "$RBE_CI_CERT" >> ./jax_rocm_plugin/ci-cert.crt
          echo "$RBE_CI_KEY" >> ./jax_rocm_plugin/ci-cert.key
      - name: Build plugin wheels
        env:
          JAX_REF: ${{ inputs.jax-ref }}
        run: |
          # When jax-ref is empty (JAX 0.9.1+ default), --jax-source-dir is
          # omitted and jaxlib is NOT built from source; consumers install
          # jaxlib from upstream PyPI. Only PJRT and plugin wheels are produced.
          # When jax-ref is set, jaxlib is built from the checked-out ./jax dir.
          JAX_SOURCE_ARG=()
          if [ -n "${JAX_REF}" ]; then
            JAX_SOURCE_ARG=(--jax-source-dir=./jax)
          fi
          python3 build/ci_build \
            --compiler=clang \
            --python-versions="${{ inputs.python-versions }}" \
            --rocm-version="${{ inputs.rocm-version }}" \
            --rocm-build-job="${{ inputs.rocm-build-job }}" \
            --rocm-build-num="${{ inputs.rocm-build-num }}" \
            "${JAX_SOURCE_ARG[@]}" \
            dist_wheels \
            --rbe \
            --builder-image="${{ inputs.builder-image }}"
      - name: Archive plugin wheels
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact-prefix }}_r${{ inputs.rocm-version }}
          path: ./jax_rocm_plugin/wheelhouse/*.whl
