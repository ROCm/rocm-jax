# WORKSPACE for pypi_jax_rocm7_pjrt
# This workspace provides the :whl target for the ROCm PJRT wheel
# by aliasing to jax_rocm_plugin

workspace(name = "pypi_jax_rocm7_pjrt")

# Reference to jax_rocm_plugin - override via --override_repository if needed
local_repository(
    name = "jax_rocm_plugin",
    path = "../..",
)
